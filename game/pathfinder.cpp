#include "pathfinder.hpp"

#include "PathHandler.hpp"
#include "Map.hpp"

bool makeCarPath(const Map& map, PathHandler<true>& path) {
    return false;
}

bool makePedestranPath(const Map& map, PathHandler<true>& path) {
    return false;
}
